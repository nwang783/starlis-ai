/**
 * Wrangler Configuration for Stateless MCP Worker
 */
 {
	"$schema": "node_modules/wrangler/config-schema.json",
	"name": "mcp-sse-google-calendar-server", // You might rename this later
	"main": "src/index.ts",
	"compatibility_date": "2024-09-23", // Keep the date that enables implicit Node.js requires
	"compatibility_flags": [
		"nodejs_compat" // Still needed for googleapis
	],
	"observability": {
		"enabled": true
	}

	// --- REMOVED durable_objects section ---
	// --- REMOVED migrations section ---

	/**
	 * Bindings (Example: KV or R2 if needed for other purposes)
	 */
	// "kv_namespaces": [],
	// "r2_buckets": [],

	/**
	 * Environment Variables & Secrets
	 * GOOGLE_CLIENT_ID/SECRET are not strictly needed for Bearer token auth,
	 * but might be useful if adding helper endpoints for initial OAuth flow later.
	 * Ensure secrets are set via `wrangler secret put`.
	 */
	// "vars": {},
}
